# CG and PCG

These are custom implementations of CG and PCG, and variants
specialized for Laplacians.  They satisfy the interfaces of all
solvers in this package.  They call BLAS when they can.
But, they also handle more general input types.


```@autodocs
Modules = [Laplacians]
Pages   = ["pcg.jl"]
Private = false
```
